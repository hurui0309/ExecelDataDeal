# 解析策略优化报告

- 优化时间: 2026-05-15 10:45:00
- 基于审查报告: review_reports/review_20260515_103000.md
- 优化轮次: 1 轮
- 优化结果: ✅ 核心问题已修复

---

## 一、问题归纳

从审查报告提取的 6 类问题：

| 问题类型 | 出现次数 | 严重程度 |
|---------|---------|---------|
| 空表入库（应SKIP） | 6 | ❌ 严重 |
| 策略选择不当 | 1 | ❌ 严重 |
| 纯数字列名 | 3 | ⚠️ 中等 |
| 多行表头子层行误入库 | 1 | ⚠️ 中等 |
| paired_row 用于目录表 | 1 | ⚠️ 低 |
| 高null率列 | 1 | ⚠️ 低（非解析问题） |

---

## 二、优化方案与执行

### 第 1 轮

**优化方案**：
1. 空表防护：orchestrator.py 中 `rows_written==0` 时自动标记 SKIP
2. Classifier SKIP 增强：明确"必看说明"/"数据说明"/"使用说明"应 SKIP
3. 纯数字列名加前缀：sanitize_column_name 中纯数字列名加 `y_` 前缀

**修改文件**：
- `data_to_db/agents/orchestrator.py`: 增加空表防护逻辑（主表+子表）
- `data_to_db/agents/classifier.py`: SKIP 判断要点增加"必看说明"等说明页
- `data_to_db/services/mysql_writer.py`: sanitize_column_name 纯数字列名加 `y_` 前缀
- `.codebuddy/skills/optimize-parse/scripts/fix_prepare.py`: 修复 PROJECT_ROOT 路径
- `.codebuddy/skills/optimize-parse/scripts/fix_verify.py`: 修复 PROJECT_ROOT 路径
- `.codebuddy/skills/run-parse/scripts/run_parse_helper.py`: 修复 PROJECT_ROOT 路径

**测试文件**：
- 统计年鉴_1949-2008_60年资料-目录.xlsx → Sheet2/Sheet3 原为空表入库
- 统计年鉴_1949-2008_1-11_全国居民消费水平及指数.xlsx → ch11_10b 多行表头
- 中国地区粮食播种、粮食产量、灾害等数据（1990-2023年）.xlsx → 必看说明+单位说明

**执行结果**：
- 处理文件数: 3
- 成功: 5, 跳过: 6, 错误: 0
- 之前 6 张空表现在被正确 SKIP

**验证结果**：

| 表名 | 之前问题 | 优化后状态 | 验证结论 |
|------|---------|-----------|---------|
| Sheet2 (目录) | SUCCESS(0行) | SKIP | ✅ 修复 |
| Sheet3 (目录) | SUCCESS(0行) | SKIP | ✅ 修复 |
| 搜索马克数据网 | 未SKIP | SKIP | ✅ 修复 |
| ch11_10b (消费水平) | 首行子层误入库 | 待验证(需重新入库) | ⚠️ 需清理旧表重跑 |

---

## 三、优化效果汇总

| 问题类型 | 修复前 | 修复后 | 状态 |
|---------|--------|--------|------|
| 空表入库 | SUCCESS(0行) | SKIP | ✅ 已修复 |
| "必看说明"未SKIP | Classifier 未识别 | SKIP 规则增强 | ✅ 已修复 |
| 纯数字列名 | 1978/1980 无前缀 | y_1978/y_1980 | ✅ 代码已修复（需新表验证） |

---

## 四、遗留问题

1. **纯数字列名**：修复代码已合入，但当前测试文件中无纯数字列名的表，需后续遇到农村年鉴类数据时验证
2. **多行表头子层行误入库**（ch11_10b 首行 All/Rural/Urban）：需清理旧表后重跑 sample_files 验证
3. **策略选择不当**（1_12_total_fixed_asset_investment 应 multi_header 但用了 simple_header）：需 Classifier 增强多行表头识别能力

---

## 五、代码变更摘要

1. **`orchestrator.py`**:
   - 主表空表防护：`rows_written==0` + `success==True` → 标记 SKIP
   - 子表空表防护：同样逻辑应用于 subtable_results

2. **`classifier.py`**:
   - SKIP 判断要点增加"必看说明"/"数据说明"/"使用说明"等说明页

3. **`mysql_writer.py`**:
   - `sanitize_column_name()` 纯数字列名加 `y_` 前缀

4. **`.codebuddy/skills/` 路径修复**:
   - `fix_prepare.py`, `fix_verify.py`, `run_parse_helper.py` 的 PROJECT_ROOT 少回退一层，已修复
